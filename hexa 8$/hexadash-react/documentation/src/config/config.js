import { themeColor } from './theme/themeVariables';

const config = {
  topMenu: false,
  rtl: false,
  darkMode: false,
  mainTemplate: 'lightMode',
  sidebarTemplate: 'lightMode',
  topBarTemplate: 'lightMode',
  footerTemplate: 'lightMode',
  themeColor,
};

export default config;
