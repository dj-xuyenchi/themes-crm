const config: {
  topMenu: boolean;
  rtl: boolean;
  darkMode: boolean;
  mainTemplate: string;
} = {
  topMenu: false,
  rtl: false,
  darkMode: false,
  mainTemplate: 'lightMode',
};

export default config;
